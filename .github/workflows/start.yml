name: programmingwithkumaresan

on:
  workflow_dispatch:

jobs:
  build:
    name: Start Building...
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      
      - name: Downloading & Installing Essentials
        run: |
          # 🚨 استبدل 'YOUR_DOWNLOADS_BAT_URL_PLACEHOLDER' برابط ملف Downloads.bat النهائي
          Invoke-WebRequest -Uri "https://www.dropbox.com/scl/fi/ldcilcnhb9zgnphjlgf0c/Downloads.bat?rlkey=t1ug6apgk936bgfk33n9hgavh&st=oojbja6f&raw=1" -OutFile "Downloads.bat"
          cmd /c Downloads.bat

      - name: Log In To AnyDesk (Configure RustDesk/VM)
        # تشغيل show.bat لضبط إعدادات RustDesk و VMQuickConfig
        run: cmd /c show.bat
        
      - name: Run Main Apps Setup and Keep Session Alive
        shell: powershell
        run: |
          Write-Host "Executing Setup.ps1 to install user applications and start the Keep Alive loop."
          # هذه هي الخطوة الأخيرة التي ستبدأ حلقة Keep Alive
          powershell -ExecutionPolicy Bypass -File "Setup.ps1"
