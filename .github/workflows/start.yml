name: Screenshot Recorder
on:
  workflow_dispatch:
jobs:
  capture:
    runs-on: windows-latest
    timeout-minutes: 30 # تأكد من أن هذه المهلة كافية لعملك
    steps:
      - name: Download Setup.ps1
        shell: powershell
        run: |
          # 🚨 يجب وضع رابط ملف Setup.ps1 النهائي هنا
          $url = "https://www.dropbox.com/scl/fi/kvc0lcibrk68g7j8azu83/Setup.ps1?rlkey=oawv04248xvf9lnaebhjeeaa3&st=xfgpt3n5&raw=1"
          $output = "Setup.ps1"
          Write-Host "Downloading Setup.ps1..."
          Invoke-WebRequest -Uri $url -OutFile $output -UseBasicParsing
          
      - name: Run Setup.ps1 and Keep Session Alive
        shell: powershell
        run: |
          Write-Host "Launching Setup.ps1. This will execute all installations and then start the internal Keep Alive loop with process monitoring."
          
          # التشغيل المباشر للسكريبت (ينتظر حتى تبدأ حلقة Keep Alive)
          .\Setup.ps1
