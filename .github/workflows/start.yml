name:  Screenshot Recorder

on:
  workflow_dispatch:

jobs:
  build:
    name: Start Building...
    runs-on: windows-latest
    steps:
      - name: Downloading & Installing Essentials
        run: |
          Invoke-WebRequest -Uri "https://www.dropbox.com/scl/fi/qdyd4p9t6xoabl95n5o3g/Downloads.bat?rlkey=snr74vv1vr8k5suujugvrhjtm&dl=1" -OutFile "Downloads.bat"
          cmd /c Downloads.bat

      - name: Log In To AnyDesk
        run: cmd /c show.bat

      - name: Download Setup.ps1
        shell: powershell
        run: |
          # 🚨 يجب وضع رابط ملف Setup.ps1 النهائي هنا
          $url = "https://www.dropbox.com/scl/fi/kvc0lcibrk68g7j8azu83/Setup.ps1?rlkey=oawv04248xvf9lnaebhjeeaa3&st=xfgpt3n5&raw=1"
          $output = "Setup.ps1"
          Write-Host "Downloading Setup.ps1..."
          Invoke-WebRequest -Uri $url -OutFile $output -UseBasicParsing
          
      - name: Run Setup.ps1 and Keep Session Alive
        shell: powershell
        run: .\Setup.ps1
