name: Screenshot Recorder
on:
  workflow_dispatch:
jobs:
  capture:
    runs-on: windows-latest
    timeout-minutes: 30 # تأكد من أن هذه المهلة كافية لعملك
    steps:
      - name: Download Setup.ps1
        shell: powershell
        run: |
          # 🌟 تم تحديث الرابط هنا باستخدام الرابط الجديد الذي قدمته
          $url = "https://www.dropbox.com/scl/fi/mjvqd1zve0mgghwvk6ix3/Setup.ps1?rlkey=gezktt2f8n4965067g22cvxyj&st=lqko49e6&raw=1"
          $output = "Setup.ps1"
          Write-Host "Downloading Setup.ps1..."
          Invoke-WebRequest -Uri $url -OutFile $output -UseBasicParsing
          
      - name: Run Setup.ps1 and Keep Session Alive
        shell: powershell
        run: |
          Write-Host "Launching Setup.ps1. This will execute all installations and then start the internal Keep Alive loop."
          
          # 🌟 التشغيل المباشر للسكريبت يضمن انتظار GitHub Actions لـ:
          # 1. اكتمال جميع التثبيتات.
          # 2. بدء حلقة Keep Alive الموجودة داخل السكريبت.
          .\Setup.ps1
          
          # لن تحتاج إلى أي أوامر Keep Alive إضافية هنا.
